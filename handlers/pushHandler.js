class PushHandler {
    static send(userId, message) {
      console.log(`Sending push notification to user ${userId}: ${message}`);
      //calling push api
    }
  }
  
  module.exports = PushHandler;
  